# 06 数据库设计

> AI生成初稿，人工确认。包含表结构、字段说明、索引设计、表间关系。
>
> 适用条件：本项目存在持久化存储时才使用。若无数据库（纯计算工具、纯前端、无状态服务等），
> 在 `ai/project-rules.md` §3 明确 `docs/06-db-design.md：省略`，初始化时直接跳过并删除本文件，不要保留空壳。
> 写法：完整表清单 + 阶段标签/状态；当前阶段表写全字段/索引，其余骨架。按 global-rules.md §8 积累式演进。

## 1. 表清单
（每张表一句话用途；每张表应可追溯到某个 REQ）

## 2. 表结构
（逐表：字段 / 类型 / 约束 / 说明）

## 3. 索引设计
（哪些字段建索引及理由）

## 4. 表间关系
（外键 / 关联关系，文字或 ER 图描述）
