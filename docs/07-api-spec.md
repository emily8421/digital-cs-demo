# 07 API设计

> AI生成初稿，人工确认。RESTful风格，统一响应格式，包含请求/响应示例与错误码。
>
> 适用条件：本项目对外提供接口时才使用。REST/HTTP 接口直接按本文件描述；
> 若为 SDK/库，可用本文件说明公开接口契约（风格不强求 RESTful）；
> 若完全无对外接口（纯内部模块、纯计算逻辑），在 `ai/project-rules.md` §3 明确 `docs/07-api-spec.md：省略`，
> 初始化时直接跳过并删除本文件，不要保留空壳。
> 写法：完整接口清单 + 阶段标签/状态；当前阶段写请求/响应，其余骨架。按 global-rules.md §8 积累式演进。

## 1. 统一约定
（统一响应格式、错误码体系、鉴权方式）

## 2. 接口清单
（逐接口：方法 + 路径 + 用途；每个接口应对应一个功能点）

## 3. 请求 / 响应示例
（逐接口的入参、出参示例）
