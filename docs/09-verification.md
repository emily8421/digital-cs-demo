# 09 验证计划（Verification）

> AI 生成初稿，**人工确认**。测试策略 + REQ→用例追溯矩阵 + 分阶段验证范围。
> 与 08（「何时做」）正交：本文回答「怎么算对」。按 global-rules §8 积累式演进。

## 1. 测试策略

- **单元测试**：覆盖可独立判定的纯逻辑——留资抽取/脱敏、路由规则解析、小结聚合、检索阈值判定。`tests/`，pytest（待确认）。
- **集成测试**：经模拟器通道投递消息，验证端到端编排（检索作答/缺口转人/留资/小结），不依赖企业微信。
- **验收测试**：对照 `docs/03-prd.md` §3 Demo 脚本逐条走查；以 02-srs 的可验证口径为判据。
- **数据夹具**：愿景种子知识（IP65/67、5050/2835、户外型号、起订量/交期/付款）、模拟群与角色（小雯/阿杰/老黄/陈总）。
- **环境**：Docker 本地（PG + 应用 + 模拟器）；企业微信相关用例在 Sprint-0 验证通过后补真实环境冒烟。
- **红线**：任何分支不得在无知识依据下生成产品参数/退换结论/进度（对应愿景「不乱猜」）。

## 2. REQ → 用例追溯矩阵（当前阶段 P1 必须有用例）

| REQ | 功能 | 阶段 | 用例（输入→期望） | 状态 |
|---|---|---|---|---|
| REQ-1 | 消息接入与归一化 | P1 | 投递文本→可读回字段齐全；切换通道不改业务 | 待实现 |
| REQ-2 | 售前单轮问答 | P1 | 预置知识下问 IP67→返回含该知识点；未覆盖→不编造、走缺口 | 待实现 |
| REQ-3 | 标准品采购 FAQ | P1 | 配置起订量 X→问「起订量」返回含 X | 待实现 |
| REQ-4 | 询盘留资 | P1 | 含手机号→生成脱敏留资；无联系方式→不产生记录 | 待实现 |
| REQ-5 | 人工转交通知（口语化、按角色） | P1 | 售前事项→送达 sales 角色并含摘要/客户标识 | 待实现 |
| REQ-6 | 知识缺口→留资+通知 | P1 | 未覆盖问题→客户侧请留资、员工侧缺口通知、缺口入库(open) | 待实现 |
| REQ-7 | 定时小结/日报 | P1 | 触发→输出含「总量/需跟进清单/分配对象」的小结 | 待实现 |
| REQ-8 | 多角色业务路由（基础） | P1 | 不同场景→分发到对应角色；改规则→分发变化 | 待实现 |
| REQ-10 | 转人工后 AI 暂停 | P1 | 会话置 handed_off→新消息不产生客户侧自动回复；解除后恢复 | 待实现 |
| REQ-12 | 语音/非文字消息处理 | P1 | 投递 voice→客户侧如实告知+员工侧提醒；不生成内容作答 | 待实现 |

> P2（REQ-9/11/13/14）与愿景（REQ-15/16/17）的用例在升阶段后于本表**原位追加**，状态保持「骨架·待细化」。

## 3. 分阶段验证范围

- **当前阶段（P1）**：覆盖 REQ-1/2/3/4/5/6/7/8/10/12（即 02-srs P1 段全部）。每条至少一条验收用例（见 §2）；03 §3 Demo 步骤 1–6 可端到端走通。
- **升阶段（P2）**：在 §2 原位追加 REQ-9/11/13/14 用例（REQ-10/12 已在 P1）；企业微信正式接入（REQ-15 选定合规通道后）补真实通道冒烟。
- **远期愿景**：REQ-16/17 待技术验证通过后才有可验证口径，届时补用例。

---

**追溯**：REQ 定义见 `docs/02-srs.md`；Sprint 见 `docs/08-dev-plan.md`；阶段范围见 `docs/03-prd.md` §3。
