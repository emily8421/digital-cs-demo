# Sprint-1 运行依赖（本机原型）
fastapi>=0.110
uvicorn[standard]>=0.27
SQLAlchemy>=2.0
psycopg[binary]>=3.1          # 连 PostgreSQL（psycopg3，对新 Python 支持更好）
pgvector>=0.3                 # PG 向量类型（SQLAlchemy Vector，配合 pgvector 扩展）
# embedding 由 docker 的 text-embeddings-inference(Tei) 服务提供，宿主用 httpx 调用（不依赖 torch）
pydantic>=2
pydantic-settings>=2
python-dotenv>=1.0

# 测试（pytest 直接用同一份依赖即可跑）
pytest>=8
httpx>=0.27
